Place the admin profile photos here if you want to self-host them:
- /admins/lio.jpg
- /admins/ellina.jpg

The app currently loads remote images first and falls back to these local paths if the remote URLs fail.
